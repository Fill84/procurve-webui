/**
 * Placeholder landing page. Task 2.10 replaces this with the real identity
 * table (hostname, model, MAC, firmware, uptime, etc.).
 */
export function IdentityPage() {
  return (
    <div className="p-6">
      <h2 className="text-lg font-semibold">Identity</h2>
      <p className="mt-2 text-sm text-neutral-600">
        Identity page placeholder — real implementation arrives in Task 2.10.
      </p>
    </div>
  );
}
