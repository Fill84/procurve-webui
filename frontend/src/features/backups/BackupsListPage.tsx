/**
 * Placeholder backups listing. Task 2.13 replaces this with the real table,
 * "backup now" action, and download links.
 */
export function BackupsListPage() {
  return (
    <div className="p-6">
      <h2 className="text-lg font-semibold">Backups</h2>
      <p className="mt-2 text-sm text-neutral-600">
        Backups page placeholder — real implementation arrives in Task 2.13.
      </p>
    </div>
  );
}
