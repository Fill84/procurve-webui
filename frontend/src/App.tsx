/**
 * With TanStack Router + file-based routing, the application root is
 * mounted by `main.tsx` via `<RouterProvider>`. This file is retained as
 * a no-op so that any stray imports continue to resolve; new code should
 * depend on the generated route tree in `routeTree.gen.ts` instead.
 */
export default function App() {
  return null;
}
